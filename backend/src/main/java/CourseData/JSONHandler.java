package CourseData;

public class JSONHandler {
  // retrieve firebase data (courses/ node and all its children)
  // parse in as a hashmap
  // parse in scraped data as a hashmap? may need a local server where to which
  // json data is uploaded
  // call guava hashmap difference to get a hashmap containing differing keys
  // use those keys to manually update those in firebase

}
